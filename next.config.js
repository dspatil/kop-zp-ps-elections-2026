/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure JSON files are included in build
  // No server-side rendering needed - fully client-rendered
}

module.exports = nextConfig

